# Deprecated: Beszel is now defined as an optional `monitoring` profile in compose.yml.
#
# Enable it only after filling BESZEL_DOMAIN, BESZEL_AGENT_TOKEN and
# BESZEL_AGENT_KEY in .env:
#
#   docker compose --profile monitoring up -d
